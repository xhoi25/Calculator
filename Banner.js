import React from 'react'
import { AppBar } from 'material-ui' ;
import { Toolbar } from '@mui/material';
import Typography from '@mui/material';
import {withStyle} from '@mui/material';

const styles=themes=>({
    root:{
        background-color:"red"
    },
    buttons:{
        display:"flex",
        
    }
})

const Banner = () => {
  return (
    <div>
      
    </div>
  )
}

export default Banner
